<Window x:Class="KoExplorer.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="名前の変更"
        Width="400" Height="130"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="新しい名前:" Margin="0,0,0,6"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Margin="0,0,0,12"
                 KeyDown="NameTextBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,8,0" IsDefault="True" Click="OK_Click"/>
            <Button Content="キャンセル" Width="75" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
